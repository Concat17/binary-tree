import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Node.styles';

const Node = (props) => (
  <div className="NodeWrapper">
    Test content
  </div>
);

Node.propTypes = {
  // bla: PropTypes.string,
};

Node.defaultProps = {
  // bla: 'test',
};

export default Node;
